name: Anti-Hunt Morning Filter

on:
  schedule:
    # Signal runs: 08:45 and 09:45 America/Chicago.
    # GitHub Actions supports timezone-aware schedules, so DST is handled automatically.
    - cron: '45 8 * * 1-5'
      timezone: 'America/Chicago'
    - cron: '45 9 * * 1-5'
      timezone: 'America/Chicago'
    # Outcome/learning run: after the 12:30 CT setup expiry.
    - cron: '15 13 * * 1-5'
      timezone: 'America/Chicago'
  workflow_dispatch:

concurrency:
  group: anti-hunt-filter
  cancel-in-progress: false

jobs:
  anti-hunt-filter:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "yfinance>=0.2.40,<0.3" "pandas>=2.0,<3" "requests>=2.31,<3"

      - name: Run anti-hunt filter
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          HEALTHCHECK_URL: ${{ secrets.HEALTHCHECK_URL }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "=============================================="
            echo "MANUAL TEST RUN"
            echo "=============================================="
            python anti_hunt_filter.py --manual
          elif [ "${{ github.event.schedule }}" = "15 13 * * 1-5" ]; then
            echo "=============================================="
            echo "AFTERNOON OUTCOME / LEARNING RUN"
            echo "=============================================="
            python anti_hunt_filter.py --resolve
          else
            echo "=============================================="
            echo "MORNING SIGNAL RUN"
            echo "=============================================="
            python anti_hunt_filter.py
          fi

      - name: Upload setup and learning snapshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: anti-hunt-snapshot-${{ github.run_id }}
          path: |
            setup.json
            learning_state.json
            learning_report.json
          retention-days: 30
          if-no-files-found: ignore

      - name: Persist learning state
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add learning_state.json learning_report.json

          if git diff --cached --quiet; then
            echo "No learning-state changes to commit."
          else
            git commit -m "chore: update anti-hunt learning state"
            git push origin HEAD:${{ github.ref_name }}
          fi

      - name: Notify on failure
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          curl -s -X POST \
            "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            --data-urlencode \
            "text=⚠️ Anti-hunt workflow FAILED (run ${{ github.run_id }}): ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
